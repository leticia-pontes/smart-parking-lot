name: Docker Check
on:
    push:
        paths:
            - "Dockerfile"
            - "docker-compose.yml"
            - "mosquitto/**"
jobs:
    build-check:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4
            - name: Build Docker Image
              run: docker build -t smart-parking-lot-test .
